// Write a program in C to add two numbers using pointers.
#include <stdio.h>

int main() {
    // Write C code here
    int a, b;
    a = 10;
    b = 20;
    int *p1 = &a;
    int *p2 = &b;
    
    printf("SUM: %d", *p1 + *p2);   //dereference and add it

    return 0;
}
